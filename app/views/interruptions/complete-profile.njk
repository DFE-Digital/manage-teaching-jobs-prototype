
{% extends "layout.njk" %}

{% if user.organisation.schools %}
  {% set title = 'Add your school logos and photos' %}
{% else %}
  {% set title = 'Add your school logo and photo' %}
{% endif %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      {% if user.organisation.schools %}
        <h1 class="govuk-heading-l">{{title}}</h1>
        <p>The details stored in your organisation profile are included in your job listings.</p>
      {% else %}
        <h1 class="govuk-heading-l">{{title}}</h1>
        <p>The details stored in your school profile are included in your job listings.</p>
      {% endif %}

      {{ govukButton({
        text: "Add logo and photo",
        href: "/organisation"
      }) }}

      <p><a href="/jobs">Skip to job listings</a></p>

      {# {% if user.organisation.schools %}
        <h1 class="govuk-heading-l">Complete your organisation profile</h1>
        <p>The details you add to your organisation profile will be included in your job listings.</p>
      {% else %}
        <h1 class="govuk-heading-l">Complete your school profile</h1>
        <p>The details you add to your school profile will be included in your job listings.</p>
      {% endif %}

      {{ govukButton({
        text: "Complete school profile",
        href: "/organisation"
      }) }}

      <p><a href="/jobs">Skip to job listings</a></p> #}

    </div>
  </div>

{% endblock %}
